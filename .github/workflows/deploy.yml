name: Deploy to Coolify

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Coolify deployment
        run: |
          curl -s "http://62.113.37.209:8000/api/v1/deploy?uuid=z16gogz2sv3y6xyr6rfg3j0d&force=false"
